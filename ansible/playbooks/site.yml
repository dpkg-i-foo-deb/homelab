---
- name: Hypervisor configuration
  ansible.builtin.import_playbook: 'hypervisor.yml'
- name: DNS servers configuration
  ansible.builtin.import_playbook: 'dns.yml'
- name: Reverse proxy configuration
  ansible.builtin.import_playbook: 'reverse_proxy.yml'
- name: Security services configuration
  ansible.builtin.import_playbook: 'security.yml'
- name: Remote access services configuration
  ansible.builtin.import_playbook: 'remote.yml'
- name: Streaming services configuration
  ansible.builtin.import_playbook: 'streaming.yml'
- name: Knowledge services configuration
  ansible.builtin.import_playbook: 'knowledge.yml'
