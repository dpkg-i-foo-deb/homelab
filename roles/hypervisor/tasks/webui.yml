- name: Enable cockpit service and ensure it is running
  ansible.builtin.systemd_service:
    name: '{{ item }}'
    enabled: true
    state: started
    masked: no
  with_items: 
    - cockpit
    - cockpit.socket